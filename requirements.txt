# Runtime dependencies for SandiRaksa.
# Kept in sync with [project.dependencies] in pyproject.toml.
# Lower bounds match pyproject; pin exactly here if fully reproducible builds
# are required for a release.
PySide6>=6.6.0
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
spacy>=3.7.0
openpyxl>=3.1.0
python-docx>=1.1.0
python-pptx>=0.6.23
lxml>=5.0.0
defusedxml>=0.7.1
cryptography>=42.0.0
keyring>=25.0.0
platformdirs>=4.0.0
pydantic>=2.5.0
charset-normalizer>=3.3.0
